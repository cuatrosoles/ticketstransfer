# Nixpacks para servicios que requieren pnpm (monorepo)
# Usar cuando Root Directory = v2 (o raíz del monorepo)

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["corepack enable && pnpm install"]

[start]
cmd = "echo 'Override Start Command en Railway Settings'"
