# Nixpacks para API (monorepo con npm)
# Solo aplica cuando Root Directory = v2 y el servicio es la API

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "echo 'Override en Railway Settings'"
