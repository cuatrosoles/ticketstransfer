# Nixpacks para API (monorepo con pnpm)
# Solo aplica cuando Root Directory = v2 y el servicio es la API

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["corepack enable && pnpm install"]

[start]
cmd = "echo 'Override en Railway Settings'"
