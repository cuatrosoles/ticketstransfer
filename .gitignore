node_modules

# Salida local para subir solo estáticos al hosting (pnpm static:prepare)
static-deploy/

# Firebase (credenciales - NUNCA subir)
firebase-service-account.json
**/firebase-service-account*.json

# Build outputs (no subir a Git)
*.apk
*.aab
apps/mobile/android/app/build/
apps/mobile/android/.gradle/
apps/mobile/ios/build/
