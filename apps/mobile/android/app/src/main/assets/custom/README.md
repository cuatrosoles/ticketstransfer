Coloca aqui las fuentes para mobile:

- `Cooper-Regular.ttf`
- `Cooper-Black.ttf`

Luego ejecuta desde `apps/mobile`:

```bash
npx react-native-asset
```

Despues recompila:

```bash
npm run android
# o
npm run ios
```
